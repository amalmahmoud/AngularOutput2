import { ButtonsBackgroundDirective } from './buttons-background.directive';

describe('ButtonsBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
